## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues (`gh` CLI), repo `khawarizmi02/expense-tracker-v2`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels, unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
